# Per-session interim work dir written by the tool when run from this dir
.codex-image-gen-tmp/

# Persistent output dir where selected images are copied (the "use these" set)
codex-image-gen-output/

# Developer-local fixtures for the edit-mode live-smoke test (tests/live-smoke.test.mjs).
# Drop in any two PNGs with these names at the repo root to enable test 63.
# Not committed because they're typically large generative PNGs.
/alien.png
/pose.png

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.eslintcache
*.tsbuildinfo

# Build / coverage / dist
dist/
build/
coverage/
.nyc_output/

# Env
.env
.env.*

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/
